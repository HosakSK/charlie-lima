"use client";

export default function AdminPage() {
  return (
    <div style={{ padding: "40px", fontFamily: "Inter, sans-serif", maxWidth: "800px", margin: "0 auto" }}>
      <h1>Admin Panel</h1>
      <p style={{ marginTop: "20px", color: "#888" }}>
        Admin panel pre správu datasetov (V2 - pripravuje sa).
      </p>
    </div>
  );
}
